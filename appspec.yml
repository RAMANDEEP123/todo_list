version: 0.0
os: linux
files:
  - source: /
    destination: /app
permissions:
  - object: /app
    pattern: "**"
    owner: ubuntu
    group: ubuntu
hooks:
  # BeforeInstall:
  #   - location: scripts/before_install.sh
  #     timeout: 300
  #     runas: root
  # AfterInstall:
  #   - location: scripts/after_install.sh
  #     timeout: 300
  #     runas: root
  #     - location: scripts/start_server.sh  # Include a script to start the server
  #       timeout: 300
  #       runas: ubuntu  # Adjust the user based on your setup
  ApplicationStart:
    - location: scripts/application_start.sh
      timeout: 300
      runas: root